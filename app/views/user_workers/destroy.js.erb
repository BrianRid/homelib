const workerFavoriteDestroy = document.getElementById('<%= @user_worker.worker.id %>');
console.log(workerFavoriteDestroy);

if (workerFavoriteDestroy) {
  <%if @user_worker.errors.any? %>
    workerFavoriteDestroy.innerHTML = '<%= j render "user_workers/worker_favorite", worker: @user_worker.worker %>';
  <% else %>
    workerFavoriteDestroy.innerHTML = '<%= j render "user_workers/worker_favorite", worker: @user_worker.worker %>';
  <% end %>
}
