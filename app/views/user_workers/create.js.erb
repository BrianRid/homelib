// Here you generate *JavaScript* that would be executed in the browser
const workerFavorite = document.getElementById('<%= @worker.id %>')

if (workerFavorite) {
  <%if @user_worker.errors.any? %>
    workerFavorite.innerHTML = '<%= j render "user_workers/worker_favorite", worker: @worker %>';
  <% else %>
    workerFavorite.innerHTML = '<%= j render "user_workers/worker_favorite", worker: @worker %>';
  <% end %>
}

