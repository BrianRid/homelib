 <div class="container d-flex text-align-center">
  <%= simple_form_for Incident.new do |f| %>
    <div class="col-8">
    <%= f.input :answer, label: "#{decision[:question] unless decision[:question] == "Voulez-vous déclarer un incident ?"}", collection: decision[:answers], as: :radio_buttons %>
    </div>
    <%= f.submit "OK", class: "btn btn-primary d-none" %>
  <% end %>
 </div>
